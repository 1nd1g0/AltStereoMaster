# Cross-Linux Requirements for StereoMaster
# This file is compatible with any modern Linux distribution
# PyTorch will be installed from the official PyTorch index with CUDA support detection
# No hardcoded versions - uses latest compatible packages

# Install PyTorch with CUDA (auto-detects available CUDA version)
# Uses dynamic index URL that adapts to system CUDA version
--extra-index-url https://pypi.org/simple

# Core PyTorch packages (version-flexible, installs latest compatible)
torch
torchvision
torchaudio

# Essential dependencies (no hardcoded versions)
argparse
opencv-contrib-python
numpy
tqdm
requests
huggingface_hub
fire
accelerate
safetensors
transformers
decord
diffusers
av
matplotlib
mediapy
opencv-python
imageio
imageio-ffmpeg
easydict
pillow
einops
pyqt5
numba
setuptools
wheel
ninja
scenedetect
color-matcher
OpenEXR
Imath

# Forward warp package (build from source on Linux)
# Note: The CUDA extension will be built during installation
# Install after other dependencies with: pip install -e dependency/Forward-Warp
# Then build CUDA extension: cd dependency/Forward-Warp/Forward_Warp/cuda && python setup.py install
